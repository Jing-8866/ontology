# ============================================
# 主配置文件
# 作用：指定当前运行环境
# 可选值：dev, prod
# ============================================

current_env: dev  # 修改为 'prod' 即可切换至生产环境

# 也可以支持通过环境变量覆盖（优先级更高）
# export APP_ENV=prod